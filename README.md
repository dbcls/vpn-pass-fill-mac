# MacOS の FortiClient VPN でパスワード自動で埋める何か

パスワード保存してくれないし、追加でトークン入力を要求してくる場合もあって手間ですね
* AI で適当に作ったので自己責任で
* FortiClient で複数の VPN 接続先を管理してる環境には未対応
  * そういう環境の人がいたらその方がアップデートしてくれるでしょう、たぶん（期待）

## 使い方

* Requirements

```
pip install rumps pyobjc
```

* Terminal で実行
  * OS 側で Terminal アプリのプライバシーとセキュリティ＞アクセシビリティの許可が必要

```
python forti_pass_fill.py > ./status.log 2>&1 &
```

* メニューバーの🐾(足跡)アイコンからパスワードを OS ののキーチェーンに保存
* メニューバーの FortiClient アイコンから "Connect to hoge" でつなげた時のみ対応
* LaunchAgent で起動時常駐化とかは自分で調べてやってください
  * Agent にしたらアクセシビリティ許可は Terminal じゃなくて Python かも
* 常時監視してるのでその分のリソースは食います
  * 監視回数変えたかったらコード書き換えてください (コードは0.4秒毎)
* 動作ログは標準出力に出るので、上記のように `> ./status.log 2>&1` でファイルに残せます

## パスワードだけ自動化
* forti_pass_fill.py
* パスワードだけ対応版
* トークンは自分でコピペしてください

## トークンも自動化（Apple mail 版）
* forti_pass_fill_applemail.py
* トークンを受け取るメールアカウント設定済みの Apple mail アプリを起動しておく
  * OS 側でアプリのプライバシーとセキュリティ＞オートメーションにおいて "Terminal &gt; Mail" の許可が必要
    * Agent 化した場合は "Python &gt; Mail"
* メニューバーの🐾アイコンからメールアカウント名を指定保存
* メール受信を待つ（安全側に倒して長めに５秒決め打ち）ので時間がかかる場合もあります
* 通知メールのパースは某組織のものに合わせてハードコードされています
* macOS のメジャーアップデート後にトークンが入らなくなったら、オートメーション許可がリセットされている可能性大
  * システム設定＞プライバシーとセキュリティ＞オートメーションで "Terminal(または Python) > Mail" を再度許可する
  * 許可切れを検知すると🐾が通知を出します

## トークンも自動化（IMAP 版）
* forti_pass_fill_imap.py
* basic 認証・アプリパスワードが許可されているアカウント環境でのみ利用可能
  * 外部アプリ不要で、ツールに IMAP アカウントを設定するだけ
* メニューバーの🐾アイコンから設定 (Gmail の例は後述)
  * "Set IMAP Account" … ホスト名 / ポート(通常 993) / ユーザー名 / フォルダ(通常 INBOX)
  * "Set IMAP Password" … メールボックスのパスワード（2段階認証環境では**アプリパスワード**）を OS キーチェーンに保存
  * "Test IMAP Login" … 接続確認（まずこれで成功するか確かめる）
* **Gmail / Google Workspace の場合のパスワード設定（アプリパスワード）**
  1. 対象の Google アカウントの**2段階認証プロセスを有効化**しておく（未設定だとアプリパスワードを作れない）
  2. https://myaccount.google.com/apppasswords を対象のアカウントで開く
     * 項目が出てこない／エラーになる場合は、管理者がアプリパスワードを封鎖済み＝この方式は使えない（Apple Mail 版を使う）
  3. 適当なアプリ名（例: `forti`）を入力して生成 → 表示される**16桁のパスワード**（空白は詰めてよい）をコピー
  4. 🐾メニューの "Set IMAP Password" にそれを貼り付けて保存
  5. "Set IMAP Account" を設定（Gmail なら ホスト `imap.gmail.com` / ポート `993` / ユーザー名はフルのメールアドレス / フォルダ `INBOX`）
     * Gmail 側の設定で **IMAP アクセスが有効**になっていること（設定 > メール転送と POP/IMAP）
  6. "Test IMAP Login" で「IMAP ログイン成功」が出れば完了
  * ※ ここで保存するのは**通常のログインパスワードではなくアプリパスワード**。通常パスワードでは弾かれる
* **動作するメール環境**
  * basic 認証 / アプリパスワードで IMAP ログインできるアカウント（大学・ISP・自前サーバ、個人 Gmail のアプリパスワード、iCloud、Fastmail など）
  * ✕ **Google Workspace / Microsoft 365 で管理者が basic 認証・アプリパスワードを無効化している組織アカウントは不可**（OAuth2 必須のため）。その場合は Apple Mail 版を使う
* 通知メールのパースは某組織仕様（件名の `AuthCode: 数字`）。送信者では絞らず件名で検索する
  * 件名で見つからなければ本文(text/plain)も走査
  * 古い使い回しを防ぐため、受信から 5 分以内のメールのみ対象、かつ最新の1通を採用

## トークンも自動化（他の手段版）
* 誰かが作るかもしれない
